export enum Status {
  ACTIVE = 'active',
  IDEAL = 'ideal',
  COMPLETED = 'completed',
  FORFEITED = 'forfeited',
}

export enum QuestionStatus {
  CORRECT = 'correct',
  WRONG = 'wrong',
  TIMEOUT = 'timeOut',
}
